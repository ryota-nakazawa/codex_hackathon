# 試験エージェントメモ

## 自動試験対象

- 空入力の拒否
- テキスト単独入力時の制約順守
- 画像単独入力時の制約順守
- `live` 未設定時の `fallback` 退避
- Cross-origin 拒否
- 非JSON Content-Type の拒否

## 手動確認観点

- 画像添付の有無で入力補助文が理解できること
- `mock`, `live`, `fallback` の切替が視覚的に分かること
- Markdown 書き出しが成功すること
- モバイル幅で入力と結果が縦並びになること
